<!-- LOGIN  -->
<div class="flex w-full justify-center" id="login">
  <section class="h-screen">
    <div class="px-6 h-full text-gray-800">
      <div class="flex justify-center items-center flex-wrap h-full g-6">
        <div class="">
          <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/draw2.webp" class="w-full max-w-lg"
            alt="Sample image" />
        </div>
        <div class="flex justify-center">

          {{#if loginForm}}
          {{> login-form-details}}
          {{/if}}

          {{#if registerForm}}
          {{> register-form-details}}
          {{/if}}

        </div>
      </div>
    </div>
  </section>
</div>
